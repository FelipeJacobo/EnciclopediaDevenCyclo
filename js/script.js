function search() {
    // Obtiene el valor del input de búsqueda con el id 'searchInput'
    const query = document.getElementById('searchInput').value;
    
    // Muestra una alerta con el término de búsqueda ingresado
    alert(`Buscando: ${query}`);
    
}
